<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<title>For Irfan 💗</title>

<style>
body {
  margin: 0;
  padding: 0;
  background: radial-gradient(circle at top,#ffe5f0,#ffd1e1,#ffb6c9,#ff9bb5,#ffeff5);
  font-family: Georgia,serif;
  display:flex;
  justify-content:center;
  align-items:flex-start;
  min-height:100vh;
  overflow-x:hidden;
}

.card {
  width:92%;
  max-width:760px;
  margin-top:30px;
  background:rgba(255,245,249,0.96);
  border-radius:18px;
  box-shadow:0 10px 40px rgba(255,75,125,0.35);
  text-align:center;
  border:1px solid rgba(255,155,181,0.8);
  padding:20px;
  position:relative;
}

/* SONG BLOCK — cute border box */
.musicBox {
  border:3px solid #ff4b7d;
  border-radius:15px;
  padding:10px;
  background:#ffe9f2;
  box-shadow:0 0 15px rgba(255,0,80,0.45);
  width:100%;
  max-width:350px;
  margin:auto;
}

/* handwritten letter images */
.letter-img {
  width:100%;
  border-radius:12px;
  margin:18px 0;
  box-shadow:0 0 18px rgba(255,100,150,0.35);
}

/* Hearts floating */
.heart {
  position:fixed;
  bottom:-20px;
  font-size:23px;
  color:#ff3b6a;
  animation:float 6s linear infinite;
  opacity:.9;
  pointer-events:none;
}
@keyframes float{
  0%{transform:translateY(0) scale(1);opacity:1;}
  100%{transform:translateY(-120vh) scale(1.6);opacity:0;}
}

/* sparkles */
.sparkle {
  position:fixed;width:7px;height:7px;border-radius:50%;
  background:white;box-shadow:0 0 12px 5px #ff7aa9;
  animation:spark 1.6s infinite ease-in-out;
  pointer-events:none;opacity:.9;
}
@keyframes spark{
  0%,100%{transform:scale(.6);opacity:.5;}
  50%{transform:scale(1.5);opacity:1;}
}
</style>
</head>

<body>
<div class="card">

  <h2 style="margin-bottom:5px;color:#ff2058;">FOR IRFAN ❤️</h2>
  <p style="color:#c03c5e;margin-top:0;">from the heart 💌</p>

  <!-- 🔥 YOUTUBE SONG BLOCK HERE -->
  <div class="musicBox">
    <iframe width="100%" height="180" style="border-radius:12px;"
      src="https://www.youtube.com/embed/koGyvheXKvM?autoplay=0&loop=1&playlist=koGyvheXKvM"
      allow="autoplay; encrypted-media">
    </iframe>
  </div>

  <p style="font-size:14px;color:#c03c5e;margin-top:10px;padding:0 12px;">
    Die with a smile is one of my favourite songs, and I always think about you while listening to it…  
    so play this while you read the letter — I wrote it listening to this 💋❤️
  </p>

  <!-- 💌 Handwritten letter page 1 -->
  <img src="IMG_1865.jpeg" class="letter-img">

  <!-- 💌 Handwritten letter page 2 -->
  <img src="IMG_1866.jpeg" class="letter-img">

</div>

<script>
document.addEventListener("DOMContentLoaded",()=>{
  // hearts
  setInterval(()=>{
    let h=document.createElement("div");
    h.className="heart";
    h.textContent="❤";
    h.style.left=Math.random()*100+"vw";
    h.style.animationDuration=(4+Math.random()*4)+"s";
    document.body.appendChild(h);
    setTimeout(()=>h.remove(),8000);
  },900)

  // sparkles
  setInterval(()=>{
    let s=document.createElement("div");
    s.className="sparkle";
    s.style.left=Math.random()*100+"vw";
    s.style.top=Math.random()*100+"vh";
    document.body.appendChild(s);
    setTimeout(()=>s.remove(),1700);
  },250)
});
</script>
</body>
</html>
